# Optional. Without these, the app stays local to this computer.
# Create a Supabase project, run the SQL in supabase/migrations, and deploy
# the functions in supabase/functions. Use the anon key, never the service role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
